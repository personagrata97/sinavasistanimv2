[{"AllowMultiple":false,"EndLine":654,"ReplacementContent":"                const { verifyNotesAgainstSource } = await import(\"@/lib/ai-service\")\n                const verification = await verifyNotesAgainstSource(\n                  section.rawContent, notes, section.title, course.name\n                )","StartLine":649,"TargetContent":"                const { verifyNotesAgainstSource } = await import(\"@/lib/ai-service\")\n                const verification = await verifyNotesAgainstSource(\n                  section.rawContent, notes, section.title,\n                  section.pageStart, section.pageEnd\n                )"}]
[{"AllowMultiple":false,"EndLine":91,"ReplacementContent":"    // Read PDF buffer\n    if (!course.pdfPath) throw new Error(\"PDF Path not found\");\n    const pdfBuffer = await readFile(course.pdfPath)","StartLine":90,"TargetContent":"    // Read PDF buffer\n    const pdfBuffer = await readFile(course.pdfPath)"}]
[{"AllowMultiple":false,"EndLine":628,"ReplacementContent":"                    const fullCourseName = `SPL Düzey ${course.level || 3} > ${course.name}`;\n                    \n                    if (isSmartInject) {\n                      const { smartInjectCourseNotes } = await import(\"@/lib/ai-service\");\n                      notes = await smartInjectCourseNotes(\n                        notes, // Eski mükemmel not\n                        feedbackItems.join(\"\\n\\n\"),\n                        section.title,\n                        fullCourseName,\n                        course.userLevel,\n                        aiMode\n                      );","StartLine":618,"TargetContent":"\n                    if (isSmartInject) {\n                      const { smartInjectCourseNotes } = await import(\"@/lib/ai-service\");\n                      notes = await smartInjectCourseNotes(\n                        notes, // Eski mükemmel not\n                        feedbackItems.join(\"\\n\\n\"),\n                        section.title,\n                        course.name,\n                        course.userLevel,\n                        aiMode\n                      );"},{"AllowMultiple":false,"EndLine":641,"ReplacementContent":"                if (!isSmartInject) {\n                  const fullCourseName = `SPL Düzey ${course.level || 3} > ${course.name}`;\n                  notes = await generateCourseNotes(\n                    enrichedContent, section.title, fullCourseName, course.userLevel,\n                    aiMode, section.pageStart, section.pageEnd\n                  )\n                }","StartLine":635,"TargetContent":"                if (!isSmartInject) {\n                  notes = await generateCourseNotes(\n                    enrichedContent, section.title, course.name, course.userLevel,\n                    aiMode, section.pageStart, section.pageEnd\n                  )\n                }"},{"AllowMultiple":false,"EndLine":654,"ReplacementContent":"                const { verifyNotesAgainstSour
<truncated 1470 bytes>
[{"AllowMultiple":false,"EndLine":797,"ReplacementContent":"                        const fullCourseName = `SPL Düzey ${course.level || 3} > ${course.name}`;\n                        const auditResult = await auditNotesAgainstSourceSpecific(\n                          fullCourseName,\n                          section.rawContent,\n                          notes,\n                          section.title,\n                          pack,\n                          section.pageStart,\n                          section.pageEnd\n                        )","StartLine":790,"TargetContent":"                        const auditResult = await auditNotesAgainstSourceSpecific(\n                          section.rawContent,\n                          notes,\n                          section.title,\n                          pack,\n                          section.pageStart,\n                          section.pageEnd\n                        )"}]
[{"AllowMultiple":false,"EndLine":475,"ReplacementContent":"          let notes = section.content || \"\"","StartLine":475,"TargetContent":"          let notes = section.notes || \"\""}]
[{"AllowMultiple":false,"EndLine":475,"ReplacementContent":"          let notes = section.notes || \"\"","StartLine":475,"TargetContent":"          let notes = section.content || \"\""}]
[{"AllowMultiple":false,"EndLine":898,"ReplacementContent":"                    if (packages.length > 0) {\n                      let overallPassed = true\n                      let allMissingDetails: string[] = []\n                      let allContradictions: string[] = []\n                      let allFindings: Array<{ description: string; severity: string; type: string }> = []\n\n                      console.log(`[BG] 📦 Toplam Paket Sayısı: ${packages.length} paket denetlenecek.`)\n\n                      let packIdx = 1\n                      for (const pack of packages) {\n                        console.log(`[BG] 👉 [Paket ${packIdx}/${packages.length}] Müfettiş inceliyor...`)\n                        await new Promise(r => setTimeout(r, 4000))\n\n                        try {\n                          const fullCourseName = `SPL Düzey ${course.level || 3} > ${course.name}`;\n                          const auditResult = await auditNotesAgainstSourceSpecific(\n                            fullCourseName,\n                            section.rawContent,\n                            notes,\n                            section.title,\n                            pack,\n                            section.pageStart,\n                            section.pageEnd\n                          )\n\n                          if (!auditResult.passed) {\n                            overallPassed = false\n                            console.warn(`[BG] ❌ [Paket ${packIdx} BAŞARISIZ]`)\n                            if (auditResult.missingDetails?.length) allMissingDetails.push(...auditResult.missingDetails)\n                            if (auditResult.contradictions?.length) allContradictions.push(...auditResult.contradictions)\n                          } else {\n                            console.log(`[BG] ✅ [Paket ${packIdx} BAŞARILI]`)\n                          }\n\n                          // Severity-weighted findings biriktir\n                          if (auditResult.findings?.length) {\n         
<truncated 12810 bytes>
